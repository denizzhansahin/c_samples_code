#include <stdio.h>
int main( ) {
char x, y[ 9 ];
printf( "Bir string girin: " );
scanf( "%c%s", &x, y );
printf( "Karakter \"%c\" ", x );
printf( " ve string \"%s\"\n", y );
printf( "\ngirdiniz.");
return 0; }
